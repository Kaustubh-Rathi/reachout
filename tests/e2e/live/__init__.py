"""Live Provider End-to-End Test Suite Package.

Explicitly dedicated to real external provider interactions (WhatsApp Web via Playwright and Live SMTP).
Never silently uses fake providers or mock doubles.
Opt-in execution requiring LIVE_E2E=1 or channel-specific flags.
"""
